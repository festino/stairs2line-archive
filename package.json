{
  "name": "stairs2line-archive",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "migrate": "node src/cli.mjs migrate",
    "resolve": "node src/cli.mjs resolve",
    "validate": "node src/cli.mjs validate",
    "build": "node src/cli.mjs build",
    "test": "node --test"
  }
}
